# Frontend Environment Variables

# API Configuration
VITE_API_BASE_URL=/api

# N8N Configuration (Optional)
#VITE_N8N_WEBHOOK_URL=https://your-n8n-domain.com:5679/webhook-test/
VITE_N8N_WEBHOOK_URL=https://your-n8n-domain.com:5679/webhook/

# Production Configuration
VITE_DEV_MODE=false

# Typewriter Animation Configuration
# Enable or disable typewriter animation (true/false)
VITE_TYPEWRITER_ENABLED=false
# Speed of typewriter animation in milliseconds per character
VITE_TYPEWRITER_SPEED=30

# Docker Configuration
# Frontend port configuration for Docker containers (avoid 8080 conflicts)
FRONTEND_PORT=8090
# Development port for Docker dev environment
FRONTEND_DEV_PORT=5173

# Backend port configuration
BACKEND_PORT=3006

# Network configuration
DOCKER_NETWORK_SUBNET=172.20.0.0/16
DOCKER_DEV_NETWORK_SUBNET=172.21.0.0/16

# Note: Backend-specific variables (database, security, N8N) are now in backend/.env
# Docker Compose will read both .env and backend/.env files