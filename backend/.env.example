# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:8080

# Database Configuration
DB_SERVER=localhost
DB_DATABASE=PersonaAILink
DB_USER=your_username
DB_PASSWORD=your_password
DB_PORT=1433
DB_ENCRYPT=false
DB_TRUST_CERT=true

# N8N Configuration (Optional)
# Primary webhook URL for N8N integration (base URL - paths will be auto-appended)
N8N_WEBHOOK_URL=https://your-n8n-server.com:5679/webhook-test/

# N8N API Key (if required for authentication)
N8N_API_KEY=

# Webhook Timeout Configuration (in milliseconds)
# Chat webhook timeout for AI responses (milliseconds)
CHAT_WEBHOOK_TIMEOUT=60000

# Training webhook timeout for model training operations (milliseconds)
TRAINING_WEBHOOK_TIMEOUT=60000

# Processing webhook timeout for file processing operations (milliseconds)
PROCESSING_WEBHOOK_TIMEOUT=60000
# Security
JWT_SECRET=your-jwt-secret-key-here
API_KEY=your-api-key-here

# SFTP Configuration
SFTP_HOST=10.60.10.44
SFTP_USERNAME=it.support
SFTP_PASSWORD=your_sftp_password
SFTP_PORT=22
SFTP_UPLOAD_PATH=/uploads

# Logging
LOG_LEVEL=info